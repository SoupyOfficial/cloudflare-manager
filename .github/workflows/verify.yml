name: Verify Configuration

on:
  pull_request:
    branches: [main]
    paths:
      - 'workers/**'
      - 'infrastructure/**'
      - '.github/workflows/verify.yml'
  push:
    branches: ['**', '!main']
    paths:
      - 'workers/**'
      - 'infrastructure/**'

jobs:
  validate-workers:
    name: Validate Workers
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Validate wrangler.toml files
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
        run: |
          for dir in workers/*/; do
            if [ -f "$dir/wrangler.toml" ]; then
              echo "Validating $dir/wrangler.toml..."
              cd "$dir"
              npx wrangler deploy --dry-run --outdir /tmp/wrangler-dry-run 2>&1 || echo "Warning: $dir validation failed (may need CF_API_TOKEN secret)"
              cd - > /dev/null
            fi
          done

      - name: Check for committed secrets
        run: |
          if grep -r "sk-" workers/ --include="*.js" --include="*.ts" --include="*.toml" 2>/dev/null; then
            echo "ERROR: Potential API keys found in source files"
            exit 1
          fi
          if grep -r "CF-Access-Client-Secret" . --include="*.json" --include="*.yaml" --include="*.yml" 2>/dev/null; then
            echo "ERROR: Potential service token secrets found"
            exit 1
          fi
          echo "No secrets detected in source files"

  validate-infra:
    name: Validate Infrastructure Configs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate JSON files
        run: |
          shopt -s globstar nullglob
          files=(infrastructure/**/*.json)
          if [ ${#files[@]} -eq 0 ]; then
            echo "No JSON files found in infrastructure/"
          else
            for f in "${files[@]}"; do
              echo "Validating $f..."
              node -e "JSON.parse(require('fs').readFileSync('$f', 'utf8'))" || exit 1
            done
          fi

      - name: Validate YAML files
        run: |
          npm install -g js-yaml
          shopt -s globstar nullglob
          files=(infrastructure/**/*.yaml)
          if [ ${#files[@]} -eq 0 ]; then
            echo "No YAML files found in infrastructure/"
          else
            for f in "${files[@]}"; do
              echo "Validating $f..."
              node -e "require('js-yaml').load(require('fs').readFileSync('$f', 'utf8'))" || exit 1
            done
          fi
