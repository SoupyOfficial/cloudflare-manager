# MACHINE COMMUNICATION CHANNEL
# ─────────────────────────────────────────────────────────────────────────────
# This file is a shared task queue between machines (Windows ↔ Mac Mini).
#
# Usage:
#   1. Write a task here on the source machine, commit + push.
#   2. Pull on the target machine and hand this file to an AI agent:
#      "Read machine-communication.txt and complete all the steps in it."
#   3. The agent updates this file with a status report, commits + pushes.
#   4. Pull on the source machine to confirm completion.
#
# Clear the task content when done — keep only this header.
# ─────────────────────────────────────────────────────────────────────────────

# AGENT TASK — Mac: add opencode-windows CORS origin and reinstall plist
# Sent from: Windows
# Date: 2026-05-29
#
# Context:
# - A new fixed Windows URL has been added: https://opencode-windows.madebysoupy.dev
# - This allows the OpenCode web UI "See Servers" feature to reach both machines
#   at separate stable URLs from a single browser session.
# - The Mac opencode service plist template was updated to include a second --cors
#   flag for the new Windows origin. The installed plist on Mac still has only the
#   old single --cors value and must be reinstalled.
# - opencode-manager plist template: config/macos/com.opencode.service.plist
# - Install script: scripts/restart-opencode.sh --install

## STEP 1 — Pull latest changes

cd ~/Documents/Development/opencode-manager && git pull
cd ~/Documents/Development/cloudflare-manager && git pull


## STEP 2 — Reinstall and reload the OpenCode launchd plist

# This substitutes __HOME__, __OPENCODE_BIN__, etc. and writes the final
# plist to ~/Library/LaunchAgents/com.opencode.service.plist, then reloads it.
cd ~/Documents/Development/opencode-manager
bash scripts/restart-opencode.sh --install

# Quick health check after restart
sleep 3
curl -s http://localhost:4096/global/health
# Expected: {"healthy":true,...}


## STEP 3 — Verify the Mac server now accepts CORS from both origins

curl -s -o /dev/null -w "%{http_code}" \
  -X OPTIONS http://localhost:4096/global/health \
  -H "Origin: https://opencode.madebysoupy.dev" \
  -H "Access-Control-Request-Method: GET"
# Expected: 204

curl -s -o /dev/null -w "%{http_code}" \
  -X OPTIONS http://localhost:4096/global/health \
  -H "Origin: https://opencode-windows.madebysoupy.dev" \
  -H "Access-Control-Request-Method: GET"
# Expected: 204


## STEP 4 — Verify the public Mac URL is still reachable

curl -sI https://opencode-mac.madebysoupy.dev/site.webmanifest | head -5
# Expected: HTTP/2 200


## STEP 5 — Report back in this file

# Append results in this format:
# DONE 2026-05-29: Mac plist reinstalled with opencode-windows CORS origin.
# Step 2 — reinstall + reload: OK; health -> {"healthy":true,...}
# Step 3 — CORS from opencode.madebysoupy.dev: 204; from opencode-windows.*: 204
# Step 4 — opencode-mac.madebysoupy.dev/site.webmanifest -> HTTP 200
#
# Context:
# - opencode.madebysoupy.dev is now routed by site-auth Worker with instance selection:
#     ?instance=mac      -> mac tunnel (working)
#     ?instance=windows  -> windows tunnel
# - Current blocker: windows path returns HTTP 530 because the windows tunnel/origin is offline.
# - Mac default is set to instance=mac so primary host stays available.
#
# DO ALL OF THE FOLLOWING ON WINDOWS:

## STEP 1 — Pull latest changes

cd ~/Documents/Development/cloudflare-manager && git pull
cd ~/Documents/Development/opencode-manager && git pull


## STEP 2 — Start Windows OpenCode service and tunnel

# Ensure OpenCode app service is running
Start-ScheduledTask -TaskName "OpenCode Service"

# Ensure windows Cloudflare tunnel for opencode origin is running
Start-ScheduledTask -TaskName "Cloudflare Tunnel - opencode-origin"

# Quick status check
Get-ScheduledTask -TaskName "OpenCode Service","Cloudflare Tunnel - opencode-origin" | Select-Object TaskName,State


## STEP 3 — Verify local Windows origin

curl -s http://localhost:4096/global/health
# Expected: {"healthy":true,...}


## STEP 4 — Verify Cloudflare tunnel connectivity for windows origin

# Tunnel ID used by windows origin in DNS records:
# 475f52fb-60a2-4177-b193-617fdc703e4e

cloudflared tunnel info 475f52fb-60a2-4177-b193-617fdc703e4e
# Expected: at least 1 active connector / edge location listed.

# Optional direct check of WS-facing tunnel hostname
curl -I https://opencode-ws.madebysoupy.dev/site.webmanifest
# Expected: not 530


## STEP 5 — Verify shared instance selector now reaches windows

curl -I "https://opencode.madebysoupy.dev/site.webmanifest?instance=windows"
# Expected: HTTP 200 and response header: x-opencode-instance: windows

# Optional sanity check for mac stays healthy
curl -I "https://opencode.madebysoupy.dev/site.webmanifest?instance=mac"


## STEP 6 — Report back in this file

# DONE 2026-05-29: Windows shared-instance routing restored.
#
# Step 1 — git pull on cloudflare-manager and opencode-manager: both already up to date.
#
# Step 2 — Scheduled tasks:
#   - OpenCode Service: Running
#   - Cloudflare Tunnel - opencode-origin: State shows "Ready" (task exits after spawning cloudflared);
#     cloudflared.exe process confirmed running (PID 4068).
#   NOTE: Task file referenced tunnel 475f52fb-60a2-4177-b193-617fdc703e4e, but the config file
#   (~\.cloudflared\opencode-config.yml) uses tunnel 9f197cb0-d197-4c7e-bed6-ddc2859336f3 (named "opencode").
#
# Step 3 — curl http://localhost:4096/global/health -> {"healthy":true,"version":"1.15.7"}
#
# Step 4 — cloudflared tunnel info 9f197cb0-d197-4c7e-bed6-ddc2859336f3:
#   Active connector: 3db3b98f-75cd-408c-9e9b-9d21bd31ecdf
#   Architecture: windows_amd64 v2026.5.0, Origin IP: 72.189.234.178
#   Edge locations: 2xmia01, 2xmia08
#
# Step 5 — instance=windows -> HTTP 200, X-Opencode-Instance: windows ✓
#           instance=mac    -> HTTP 200, X-Opencode-Instance: mac    ✓
